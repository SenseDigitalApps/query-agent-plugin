/**
 * Segunda extension del paquete: las herramientas con las que el agente
 * consulta Query. Va aparte del canal para que registrar herramientas no toque
 * la conexion que ya entrega los mensajes.
 */
export { default } from "./src/query-tools.js";
